# Angelscript User Widget
- `AUW` is the abbreviation of `Angelscript User Widget`.
- Every `AUW` class has a Widget Blueprint named `WBP_<ClassName>` associated with it.
- `WBP` for design, and `AUW` for implementation.
